# Taobao Insight 环境配置模板

# [必填] Google Gemini API ��钥
GEMINI_API_KEY=your_gemini_api_key_here

# [可选] Gemini API 代理端点 (国内环境请填写)
GEMINI_PROXY_URL=http://127.0.0.1:7897

# [可选] Gemini 分析使用的模型
GEMINI_FLASH_MODEL=gemini-flash-latest
# [可选] Gemini 最终生成结论使用的模型
GEMINI_PRO_MODEL=gemini-3.1-pro-preview

# [可选] API 超时时间设定 (秒)
GEMINI_TIMEOUT_SEC=180

# ----------------------------------------------------- #
# 浏览器模式配置
# ----------------------------------------------------- #

# 浏览器模式：persistent (自动启动浏览器并保持登录态)
# persistent 模式会自动管理浏览器，无需手动启动 Chrome
TAOBAO_BROWSER_MODE=persistent

# 回退机制关闭
USE_BACKEND_CRAWLER=0
ALLOW_HTTP_FALLBACK=0

# Shop filtering for search stage: on/off (default on)
SHOP_FILTER=on

# Concurrency controls
CRAWL_WORKERS=1
LLM_WORKERS=8
LLM_WORKERS_MIN=4
